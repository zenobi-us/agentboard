# Review: ClankPipe rename for issue #63

## Scope

- Renamed workspace package directories from `agentboard-*` to `clankpipe-*`.
- Renamed package names and imports from `@agentboard/*` to `@clankpipe/*`.
- Changed the plugin keyword to `clankpipe-package`.
- Changed the CLI Moon project ID to `clankpipe`.
- Updated workspace examples, plugin `uses` values, package paths, and `bun.lock`.
- Updated store hash expectations for the renamed package identity.

## Validation

- `bun install`: passed.
- `moon query projects`: passed. The CLI project is `clankpipe`.
- `moon run :typecheck`: passed.
- `moon run clankpipe:build`: passed.
- `moon run :test`: passed.
- `bun test apps/cli/src`: passed. 95 tests passed.
- `moon run docs:build`: failed on existing fixture JSON files under `clankpipe-action-llm/e2e`. Fumadocs reads these files as metadata and rejects arrays or invalid JSON. This failure is outside the package rename logic.
- `git diff --check`: passed.
