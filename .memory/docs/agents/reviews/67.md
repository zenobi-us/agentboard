# Review: ClankPipe repository migration (#67)

- Issue: https://github.com/zenobi-us/clankpipe/issues/67
- Branch: `clankpipe/zenobi-us-clankpipe-67`
- Repository migration: `zenobi-us/agentboard` -> `zenobi-us/clankpipe`

## Acceptance criteria

- Repository renamed with GitHub redirect preserved.
- Automation queries use `zenobi-us/clankpipe` and `clankpipe:*` workflow labels.
- Migration creates replacement labels before removing old labels, preserving issue state.
- New workflow branches use `clankpipe/*`; existing `agentboard/*` branches remain valid.
- Docs, demo setup, release manifest fallback, and GitHub Pages target use ClankPipe repository names.
- Apply and rollback procedure documented in `.memory/docs/agents/repository-migration.md`.

## Validation

- `bash -n .github/tasks/migrate-to-clankpipe.sh apps/demo/setup.sh` — PASS
- `.github/tasks/migrate-to-clankpipe.sh` — PASS (dry-run inspected; scans open issues only)
- `bun install --frozen-lockfile` — PASS
- `moon run clankpipe:check` — PASS
- `moon run clankpipe:build` — PASS
- `moon run clankpipe:test` — PASS (111 tests)
- `moon run docs:test` — PASS (2 tests)
- `moon run docs:build` — PASS
- `moon run docs:typecheck` — PASS
- `git diff --check` — PASS
- Review fixes: workflow prompts use `clankpipe:*` retry labels; migration scans open issues only.
- Final spec review: rollback now restores the repository, workflow configuration, remote, and original label forms.
- Final standards review: no hard violations found. The unused `run()` helper was removed.
