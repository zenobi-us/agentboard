# Issue 61 review

## Scope

Issue 61 centralizes shared TypeScript and end-to-end Moon tasks.

## Changes

- Added `.moon/tasks/typescript.yml`, inherited by projects tagged `typescript`.
- Added `.moon/tasks/e2e-testing.yml`, inherited by projects tagged `e2e-testing`.
- Tagged all TypeScript packages and tagged `agentboard-action-llm` for end-to-end tests.
- Removed package-level `package.json` scripts from `pkgs`.

## Verification

- `moon query tasks --project agentboard-core --id ts-typecheck`
- `moon query tasks --project agentboard-action-llm --id e2e`
- `moon run agentboard-core:typecheck`
- `moon run agentboard-action-llm:e2e`
- `git diff --check`
