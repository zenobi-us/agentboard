# Review: Issue 65

Base: `f01489f`
Commit: `6ea3bb3`

## Standards

- Possible duplicated migration-path logic across CLI config, Plugin discovery, and Store reads. This is a judgement call. The paths represent separate boundaries and no shared abstraction was added.
- Possible duplicate legacy Store records when both old and new paths contain the same observation. Fixed by de-duplicating current Snapshot Items by Item ID.

## Spec

The first review found two partial requirements:

- Legacy Store filenames use old Plugin identities. The first implementation only read the old directory with new filenames. Fixed by reading current and legacy Plugin identity variants.
- Bun does not resolve a dynamically changed `NODE_PATH` in this executable Workspace import path. Fixed by temporarily linking global Plugin Packages into the Workspace `node_modules` path, then removing only those links.

## Validation

- `bunx tsc -p apps/cli/tsconfig.json --noEmit`
- `bun test`
- Result: 141 tests passed.
- No automatic data move occurs.
