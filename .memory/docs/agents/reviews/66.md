# Issue 66 review

## Scope

Rename public AgentBoard names to ClankPipe in documentation, demo files, links, package metadata, release metadata, and release fixtures.

## Review results

- Standards review found one issue. The demo setup script checked for `clankpipe`, `pi`, and `zellij`, although setup only uses `curl`, `git`, `gh`, `jq`, `bun`, and `tar`. The extra checks were removed.
- Spec review found that the new repository and docs URLs do not resolve while GitHub still hosts `zenobi-us/agentboard`. The repository rename is an external action. This change keeps the requested future ClankPipe URLs.
- The generated release manifest uses the current issue worktree ref. The worktree branch keeps the repository workflow prefix `agentboard`. The source commit and repository fields use ClankPipe names.
- The first PR check failed because CI did not install `actionlint`. The root `.prototools` now pins `actionlint` 1.7.9 and registers its local Proto plugin.
- A follow-up review found stale ClankPipe package documentation and invalid action identifiers in the docs homepage example. The docs now use scoped action package names and ClankPipe terminology.
- `actionlint` then found a shell variable name mismatch in `.github/workflows/release.yml`. The workflow now uses `BASE_REF_INPUT` consistently.
- The final standards review found no documented standard violations or judgement-call code smells.
- The final spec review noted that generated release metadata contains the current `agentboard/...` worktree ref, demo issue identifiers use `AB-DEMO-*`, and future ClankPipe links depend on the external repository rename.
- The generated ref and demo identifiers are internal metadata. The PR documents the external rename dependency. No code change is required for this issue.

## Validation

- `moon run clankpipe:typecheck`
- `moon run clankpipe:build`
- `moon run clankpipe:test`
- `moon run docs:test`
- `moon run docs:typecheck`
- `moon run docs:build`
- `moon run github:test`
- `cd apps/demo && bun run lint`
- `sh -n apps/demo/setup.sh`
- `bash -n apps/demo/launch-agent.sh`
- `git diff main...HEAD --check`
