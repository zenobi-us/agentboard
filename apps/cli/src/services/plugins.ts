/**
 *
 * A plugin Service that loads agentboard plugins from an installed node_modules dir.
 *
 * use pi-coding agent as inspiration for how to load plugins from node_modules and register them with the CLI.
 */
